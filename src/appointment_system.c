#include "appointment_system.h"
#include "file_reader.h"
#include <stdlib.h>
#include <stdio.h>

int patient_creation(int const *CPR){
    if(CPR == NULL){

    }
    char test[] = "This is a test";
    printf("I am here!");
    append_entry("test_db.txt",test);

    return 0;
}